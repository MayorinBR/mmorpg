# Other Five Classes — Shared Systems Plan

Source: [iRO Wiki — Mage](https://irowiki.org/wiki/Mage), [Merchant](https://irowiki.org/wiki/Merchant), [Acolyte](https://irowiki.org/wiki/Acolyte), [Thief](https://irowiki.org/wiki/Thief), [Archer](https://irowiki.org/wiki/Archer), consulted September 2026. Companion to `CLASSES_SWORDMAN.md` (same per-class-series intent, see its own header), but scoped differently: this is an architecture plan across all five remaining base classes, not a single class's implementation record. Purpose: find which skill mechanics repeat across classes so the shared system gets built once, not re-derived per class when each one's actual implementation pass happens. **Correction, 2026-09-14: this document originally claimed nothing in it was implemented yet, which was wrong — `CLASSES_SWORDMAN.md`'s own pass (dated two days earlier) had already shipped items 1 (status effects, stun-only), 2 (buff payload) and the HP regen-over-time system this document's item 1 also depends on, and this document's own text simply hadn't been reconciled against that when it was written.** See item 1 below for the corrected status, and the "Mage damage kit"/"Mage kit — extended", "Archer kit", "Acolyte kit" and "Thief kit" sections for the skills actually authored against this plan.

## Already covered by existing systems (data only, no new code)

Skills whose mechanic already has a home and just need authoring as an asset once that class is actually implemented:

- **Level-scaling single/multi-hit damage** (`SkillEffectType.Damage`, `CalculateDamage` = stat × multiplier × level): Fire Bolt, Cold Bolt, Lightning Bolt (Mage — "launch 1~level bolts, each MATK×1" is exactly this formula, multiplier 1; **now authored as assets, see "Mage damage kit" below**), Soul Strike, Holy Light (Acolyte — still blocked on item 9, Holy element), Double Strafe (Archer — **now authored, see "Archer kit" below**), Envenom's damage half (Thief — **now authored, see "Thief kit" below**), Stone Fling, Sand Attack (Thief — **now authored, see "Thief kit" below**). No new mechanic, just tuning + an `Element` value (see the open question below).
- **Mammonite** (Merchant) — a normal single-target Damage skill; already authored (`Mammonite.asset`, registered in `MerchantBase.asset` and the master `SkillDatabase.asset`); only the Zeny-cost field below is still missing.
- **Weapon-gated flat ATK passive** (`PlayerPassiveSkillController`, `SkillDefinition.PassiveRequiredWeaponSubtypes`) — no new instance of this among these five classes' passives (Thief/Archer passives below need a *different* generalization, not this one as-is — **that generalization shipped, see item 10**).
- **Mage's `PlayerElementController`** already exists (selectable attack element, currently "purely informational" per its own doc comment) — Fire Bolt/Cold Bolt/Lightning Bolt/Fire Ball/Fire Wall don't need it: each is its own skill asset with its own fixed `Element`, independent of the auto-attack element toggle. `PlayerElementController` stays relevant only for the Mage's basic (non-skill) attack.

## New shared systems needed (priority order — most skills unblocked per build first)

### 1. Status effects — IMPLEMENTED, widened beyond stun, 2026-09-17
`StatusEffectController` (`Project.Combat`) now holds six independent timed flags instead of a single stun timer: `IsStunned`/`ApplyStun` (unchanged), plus `IsPoisoned`/`ApplyPoison`, `IsSilenced`/`ApplySilence`, `IsBlinded`/`ApplyBlind`, `IsFrozen`/`ApplyFreeze`, `IsPetrified`/`ApplyPetrify`. Stun, Freeze and Petrify all fully immobilize in real RO, so they're additionally exposed as one `IsImmobilized` convenience property for every gate that doesn't care which of the three caused it. Each expire time is tracked independently (same "extend, don't shorten, a re-application that would end sooner" rule `ApplyStun` already had); re-applying Poison keeps the stronger of the two tick amounts while it's still active, or starts fresh at the new amount once the old one expired.

**Gates wired at the existing choke points**, matching the original plan's shape:
- **Movement** (`CharacterMovementController.Update`) and **basic attack** (`PlayerCombatController.CanAttack`) both now check `IsImmobilized` — a new optional `statusEffects` hook on each, same pattern as `buffs`/`defensiveStatsSource` elsewhere. **Correction**: this document previously claimed stun was "wired as an optional hook on both the player and `EnemyController`" — that was only ever true for the enemy side (`EnemyController.Update`, which already gated its whole AI tick + `NavMeshAgent` on `IsStunned`); nothing on the player actually checked it before this pass. Fixed as part of widening rather than left as a latent gap.
- **Skill cast** (`PlayerSkillCaster.TryCastSkill`/`TryCastSkillAtPosition`) gained the same optional `statusEffects` hook, checked via a new private `IsCastBlocked()` — blocked by `IsImmobilized` (Stun/Freeze/Petrify, same as any other action) or by `IsSilenced` alone (skill casts only; movement and basic attacks stay unaffected by Silence, matching real RO).
- **Poison's damage-over-time** ships inside `StatusEffectController` itself rather than reusing `PlayerHealthRegenController`'s primitive as originally sketched — regen and poison tick in opposite directions and don't share enough shape to be worth forcing into one component. A new optional `health` hook (`HealthComponent`) is ticked on its own `poisonTickIntervalSeconds` timer, calling `TakeDamage` with the tick amount recorded by `ApplyPoison`, `Element.Neutral`, `DamageCategory.Magical` — so it's mitigated by the target's magical defense like any other Magical hit, a documented simplification since real RO poison ignores defense entirely and this project has no "true damage" path yet.
- **Blind is the one exception**: the flag and `ApplyBlind` exist, but nothing reads `IsBlinded` yet (`ponytail:` comment on the property) — real RO's Blind sharply reduces Hit, but wiring that means touching every `HitChanceCalculator.RollHit` caller (player basic attack, skills, enemy attacks) for a status that Sand Attack now applies but nothing yet reads. Deferred until Blind's accuracy penalty is actually needed, same reasoning as Arrow Repel's knockback half.

This unblocked, and as of 2026-09-18 all now ship: **Stone Curse** (petrify — see "Mage kit — extended" below; element-conversion-to-Earth still not modeled), **Frost Diver** (freeze — see "Mage kit — extended" below; element-conversion still not modeled), **Stone Fling**/**Sand Attack** (stun/blind chance — see "Thief kit" below), and **Envenom's poison half** (see "Thief kit" below, still plain `Physical`/`Neutral` damage until item 9 decides the Poison element). Still open: **Cure**/**Detoxify** (clear a status rather than inflict one — needs a `Clear(StatusEffectType)`-style method and a new cast path, neither built yet, since every mechanism shipped so far only ever *applies* a status).

**Wiring audit, 2026-09-17**: before starting item 7, checked every `StatusEffectController` consumer in the codebase and the Player/enemy scene and prefab data for gaps. Every code-side gate (movement, basic attack, skill cast, the enemy AI tick) is wired consistently — confirmed by grepping every `IsStunned`/`IsImmobilized`/`IsSilenced`/etc. reference project-wide, no stale call sites left pointing at the old stun-only API. **Resolved, 2026-09-18: Victor confirmed both the Player and enemies already have `StatusEffectController` wired in the Editor** (the component + its optional-hook fields on `PlayerCombatController`/`CharacterMovementController`/`PlayerSkillCaster`/`EnemyController`), so every gate documented above — and every status-inflicting skill authored since (Envenom, Stone Curse, Frost Diver, Sand Attack, Stone Fling) — is live at runtime, not just ahead of authoring.

### 2. Generalized buff/debuff payload — IMPLEMENTED
`BuffController` now carries a `BuffPayload` (`Assets/Scripts/Combat/BuffPayload.cs`): the original AtkPercent/DefPercent/MdefFlat trio, plus AspdPercent, MaxHealthFlat, and flat STR/AGI/VIT/INT/DEX/LUK. The old `ActiveBuff`/`PersistentModifier` structs and separate `Sum`/`SumPersistent` helpers collapsed into one `Total` property (`BuffPayload`'s own `+` operator sums the active list and the persistent dictionary); `ApplyBuff`/`SetPersistentModifier` now take a `BuffPayload` instead of a handful of loose floats/ints. `PlayerSkillCaster.TryCastBuff` builds the payload from three new `SkillDefinition` fields (`buffStatBonusPerLevel: StatModifiers`, `buffAspdPercentPerLevel`, `buffMaxHealthPerLevel`); `PlayerBerserkController` and `PlayerStatsController` were updated to the new signatures. `PlayerStatsController` folds a buff's STR/AGI/VIT/INT/DEX/LUK component into `effectiveStats` through a second, reused `JobBonusStatsView` layer (the same class already used for the Job Level bonus — no new decorator type needed), multiplies `CurrentSubStats`' Aspd by `BuffController.AspdMultiplier` the same way it already did for ATK, and adds `BuffController.MaxHealthBonus` in `GetMaxHealthBonus`.

One deviation from the original plan below: `BuffPayload` carries raw `int`/`float` fields rather than an actual `StatModifiers` value. `Project.Combat` cannot reference `Project.Items` (`StatModifiers`'s assembly) without creating a cycle — `Project.Items` already references `Project.Combat`. The conversion from `BuffPayload`'s raw fields to a real `StatModifiers` happens in `PlayerStatsController.ToStatModifiers`, which sits at the top of the assembly graph and can see both. `StatModifiers`'s constructor was made public and a `*(StatModifiers, int)` operator was added so `SkillDefinition.GetBuffStatBonus` can scale a per-level value the same way the other per-level getters do.

**Improve Concentration** (Archer) was the first buff skill authored against this infrastructure — see "Archer kit" below, including a documented gap: this payload has no percentage-stat channel, so real RO's +DEX/AGI% is shipped as a flat `buffStatBonusPerLevel` bonus instead. **Blessing, Increase AGI and Angelus** (Acolyte) now follow the same pattern — see "Acolyte kit" below.

**Bug found and fixed, 2026-09-17**: `SkillTargetType.Ally`'s own doc comment always claimed it "currently behaves the same as Self" until real ally selection exists, but `PlayerSkillCaster.TryCastBuff` never actually implemented that fallback — its ternary only special-cased `Self`, so an Ally-targeted buff fell into the Enemy-target branch instead (`targetSelector.CurrentTarget?.GetComponentInParent<BuffController>()`), which would silently fail (or worse, buff whatever enemy happened to be selected) instead of buffing the caster. Fixed by widening that condition to `TargetType == Self || TargetType == Ally`, making the code match the doc comment that was already there. This is what let Blessing/Increase AGI/Angelus below ship as real `Ally`-targeted assets instead of being mislabeled `Self`.

Original proposal (superseded by the above, kept for context): extend the buff payload to carry a flat `StatModifiers` delta (reusing the struct `ItemDefinition.StatBonuses`/`EquipmentManager.GetBonus` already use — same shape, no new type) alongside the existing Atk%/Def%/Mdef channels, plus add Aspd% and MaxHealth-flat channels since both recur here. Keep the existing timed-list + persistent-dictionary dual design (already proven by Provoke and Berserk) — nothing about *how* a buff expires or gets replaced needs to change, only *what* it can carry.

### 3. Party support (multi-target buff propagation)
No party/group system exists anywhere in this project today (confirmed — not mentioned in `FUTURE_IMPROVEMENTS.md` at all). Blocks real multi-target **Blessing**, **Increase AGI**, **Angelus** and Merchant's **Crazy Uproar** — the first three are now shipped as self-only casts instead (`Ally` target resolving to the caster, see item 2's bug note and "Acolyte kit" below), a deliberate stopgap the original text below already anticipated. This is still the biggest open prerequisite in this whole plan — Acolyte's real-RO identity is almost entirely party support, so whenever a real party model is built (even just "every other nearby player," since this project has no confirmed multiplayer grouping UI yet either), these three buffs are the ones to wire real propagation into first.

### 4. Stealth / detection (Hide–Reveal) — IMPLEMENTED, 2026-09-18, extended the same day
`StatusEffectController` gained an untimed `IsHidden` flag, `SetHidden(bool)`/`Reveal()`, deliberately not a timed expire-time field like every other status in item 1 — real RO's Hiding is a toggle, not a duration.

**Gated at the two places enemy AI acquires a target**, the same "choke point" approach item 1 used: `EnemyController.DetectPlayer()`'s fresh proximity scan now skips any hit whose player is hidden (a new `internal static bool IsPlayerHidden(Transform player)` helper — `GetComponentInParent<StatusEffectController>()` then `IsHidden`, null-safe so a player with no `StatusEffectController` wired is simply never considered hidden), and `EnemyWanderState.TryReaggroRememberedPlayer` (a Passive mob's memory-based re-engage of `RememberedAggressor`, which bypasses `DetectPlayer` entirely) checks the same helper before re-engaging. `EnemyChaseState` (an already-active chase) is deliberately untouched — see below.

**Hiding (Thief) toggles it**, see "Thief kit" below — the first real consumer, and the reason `SkillEffectType` gained a `Toggle` value. `EnemyChaseState` doesn't check `IsHidden`, so a mob already mid-chase when its target goes hidden keeps chasing instead of losing it — real RO's Hiding does break an active chase, but that needs a design decision (give up immediately, or keep moving toward the target's last known position?) that's better made once it's actually testable in-Editor (it now is, via Hiding) than guessed blind. **Resolved, 2026-09-18: the Boss/Insect/Demon exception (item 8, Race) is now built and wired** — `EnemyController.SeesThroughHiding` (true for a Boss-flagged enemy, or Insect/Demon race) is checked alongside `IsPlayerHidden` at both call sites above, so an enemy that should see through Hiding in real RO now does. **Editor wiring confirmed done, 2026-09-18** (see item 1's wiring audit) — Hiding is live at runtime.

**Reveal shipped as its own mechanic, 2026-09-18**: `SkillEffectType` gained a `Reveal` value and `PlayerSkillCaster` a new `TryCastReveal(skill, level)` — an `OverlapSphere` around the caster (reusing `SkillDefinition.AreaRadius` and `enemyLayer`, the same lookup `TryCastAreaDamage` already used) that calls `StatusEffectController.Reveal()` on every hidden target found, and — only for a target it just revealed — optionally also deals damage through the same hit-roll/size-scaling path as a Damage skill. Whether a Reveal skill also damages is decided purely by `CalculateDamage` returning non-zero, no separate flag: Sight's `damageMultiplierPerLevel` is 0 (reveal only), Ruwach's isn't (reveal and damage), matching how the two actually differ in real RO. This is `Reveal()`'s first caller. **Sight (Mage)** and **Ruwach (Acolyte)** are both authored against it — see "Mage kit — extended" and "Acolyte kit" below. `EnemyChaseState`'s reveal-during-chase gap (previous paragraph) is unaffected by this — Reveal only clears the hidden flag, it doesn't touch AI targeting directly.

### 5. Ground-targeted persistent zone skills — IMPLEMENTED (damage-on-stay case), now also authored for Fire Wall
`SkillTargetType` gained a `Ground` value and `SkillEffectType` gained `Zone`. A Ground skill never has an "already selected" position the way an Enemy target can already be selected, so `PlayerSkillCaster.TryCastSkill` always routes it into `SkillTargetingController`'s picking flow (`BeginPicking`), same as a Damage skill with no valid target. `SkillTargetingController` was extended with a second picking mode alongside its existing enemy-picking one: when the pending skill is Ground-targeted, it raycasts the mouse against a new `groundLayer` every frame instead of `enemyLayer` (`HoveredGroundPointChanged` mirrors `HoveredEnemyChanged` for UI), and right-click calls the new `PlayerSkillCaster.TryCastSkillAtPosition(skill, position)` directly instead of going through `PlayerTargetSelector` — a ground point isn't a combat target.

`TryCastSkillAtPosition` mirrors `TryCastSkill`'s learned/cooldown/dead checks, then `TryCastZone` spends mana and spawns a new `SkillZoneController` (`Assets/Scripts/Combat/SkillZoneController.cs`) at the picked position via `SkillZoneController.Spawn(...)`/`Initialize(...)`. The zone ticks on its own interval (`SkillDefinition.ZoneTickIntervalSeconds`) for its duration (`GetZoneDuration(level)`), each tick rolling a fresh `Physics.OverlapSphere` against a target layer (reusing `PlayerSkillCaster`'s existing `enemyLayer` field — no new one needed) and applying a fixed damage/accuracy/element/category baked in at cast time, the same "resolve once at cast time" approach every other skill here already uses. Reused `SkillDefinition.AreaRadius` for the zone's radius and the existing Damage-header fields (`damageMultiplierPerLevel`, `damageType`, `element`, `accuracyBonusPerLevel`) for its per-tick damage, rather than adding parallel Zone-specific copies. New Zone-only fields: `zonePrefab` (optional visual, null = logic-only), `zoneDurationSeconds`/`zoneDurationPerLevel`, `zoneTickIntervalSeconds`.

Two deliberate scope decisions:
- `SkillZoneController` lives in `Project.Combat`, so it does **not** apply `WeaponSizeModifiers` (that's `Project.Items`, downstream of `Project.Combat` — same assembly-cycle constraint as item 2's `BuffPayload`) — marked with a `ponytail:` comment. This matches real RO anyway: Fire Wall is spell damage, not a weapon hit.
- This unblocks **Fire Wall** (Mage) — a pure "damage anything standing inside" zone — end to end, **now authored as `FireWall.asset`, see "Mage damage kit" below**. It does **not** yet unblock **Safety Wall** (blocks melee) or **Pneuma** (Acolyte, blocks ranged): "block an incoming attack" is a fundamentally different mechanic than "deal damage on a tick" — it needs an interception point in the attack-resolution path (`PlayerCombatController.DealHit`, `PlayerSkillCaster`'s damage methods, and the mirrored enemy-attacks-player path), and no such hook exists anywhere in this codebase yet. Building it now would be speculative, and it's cleaner to design once the actual melee/ranged distinction at attack time is needed for something. The zone spawn/lifetime/detection core built here (`SkillZoneController`, the ground-picking flow) is the reusable half; Pneuma will need a small additional "does a zone block this attack" query layered on top.

### 6. Instant AoE centered on a target/location (not the caster) — IMPLEMENTED, now also authored for Fire Ball, Thunderstorm and Arrow Shower
`SkillTargetType` gained an `AreaAroundTarget` value alongside the existing `AreaAroundCaster` (Magnum Break). `PlayerSkillCaster.TryCastDamage` still requires a valid target in range for `AreaAroundTarget` (same range/mana gate as a single-target skill, via `HasValidDamageTarget`), then bursts on it instead of hitting only it. The actual "hit everyone within `AreaRadius`" loop — previously `TryCastAreaDamage`'s own body — was extracted into a shared `ApplyAreaDamage(skill, level, center)`, called with `transform.position` for `AreaAroundCaster` and the resolved target's position for `AreaAroundTarget`; no duplicated hit-roll/damage code between the two. `SkillDefinition.IsAreaOfEffect` still gates whether a Damage skill is an AoE at all (kept as a separate bool rather than derived from `TargetType`, since nothing guarantees existing skill assets — e.g. Magnum Break's — already have `TargetType` set to match; safer to leave that bool as the single source of truth for now), while `TargetType` now decides *where* the AoE is centered and whether a target must be selected first.

This unblocked **Fire Ball**, **Thunderstorm** (Mage, see "Mage damage kit") and **Arrow Shower** (Archer, see "Archer kit"). `TargetType.AreaAroundCaster` combined with `SkillEffectType.Buff` (rather than Damage) was also reused, unmodified, by item 8's **Signum Crucis** — see "Acolyte kit" below.

### 7. Chance-based skill effects (success rate independent of Hit/Flee) — IMPLEMENTED, 2026-09-17
`SkillDefinition` gained a single `successChancePerLevel` field (`GetSuccessChance(level)`, exactly the plain-`Random.value`-roll shape the original text below proposed — no new RNG abstraction), reused by two different mechanics depending on `EffectType` rather than two separate fields:

- **Damage skills**: a new `InflictedStatus` field (`StatusEffectType`, see item 1) names a status the skill has a chance to inflict on a hit target, alongside three matching fields — `InflictedStatusDurationSeconds`/`PerLevel` and `PoisonDamagePerTickPerLevel` (only meaningful when `InflictedStatus` is Poison). `PlayerSkillCaster` gained `TryProcInflictedStatus`, called right next to the existing `TryProcStunAugment` at both Damage hit-resolution points (`TryCastDamage`'s single-target branch and `ApplyAreaDamage`'s loop) — rolls `GetSuccessChance(level)` and, on success, calls the target's `StatusEffectController.Apply(InflictedStatus, duration, poisonDamagePerTick)`, a new generic dispatcher on `StatusEffectController` itself that switches to the matching `ApplyStun`/`ApplyPoison`/etc. so callers never need their own switch. **A zero-damage Damage skill still procs this** — `TryCastDamage`/`ApplyAreaDamage` call `TryProcInflictedStatus` unconditionally once the accuracy roll lands, regardless of how much damage was actually dealt (`HealthComponent.TakeDamage` just no-ops on a non-positive amount) — which is what let **Stone Curse** ship as a pure "chance to petrify, zero damage" skill with no code changes (`damageMultiplierPerLevel: 0`), see "Mage kit — extended" below.
- **Enemy-targeted Buff skills (debuffs)**: `TryCastBuff` now rolls `GetSuccessChance(level)` before applying the buff, but only on the non-Self/Ally branch (a self/ally buff always lands, matching real RO) — a resisted debuff still spends mana and starts cooldown, the same "resource spent even on a miss" convention `TryCastDamage` already established for a dodged hit. `GetSuccessChance` defaults to zero for every skill authored before this field existed (Provoke, Endure, Blessing, etc.), and zero is treated as "unconditional, skip the roll" rather than "never succeeds" — so nothing already shipped changed behavior. **Extended, 2026-09-18**: the same resist roll is rolled independently per target by `PlayerSkillCaster.TryCastAreaDebuff` (item 8's Signum Crucis) — see that item and "Acolyte kit" below.

This unblocked, and as of 2026-09-18 all now ship: **Envenom**'s poison-inflict chance, **Frost Diver**'s freeze-inflict chance, **Stone Curse**'s petrify-inflict chance, **Stone Fling**/**Sand Attack**'s stun/blind-inflict chance, and **Signum Crucis**'s per-target resist roll — see "Thief kit", "Mage kit — extended" and "Acolyte kit" below. **Not covered**, deliberately: **Double Attack** (a proc chance on the player's own *basic* attack, not a skill cast — `PlayerCombatController`'s attack flow has no second-hit mechanic to gate yet, a different shape of problem than "does this skill's extra effect land") and **Steal** (RO's own steal-chance-vs-monster formula, plus needs loot/inventory access a skill cast doesn't have) — both stay class-exclusive future work, per the table below.

### 8. Monster classification: Race — IMPLEMENTED, 2026-09-18 (pairs with the already-tracked Size gap)
`CLASSES_SWORDMAN.md` item 5 already shipped monster **Size** (unblocking the weapon-vs-size damage table). This pass adds the matching **Race** axis, mirroring Size's own shape exactly: a new `MonsterRace` enum (`Assets/Scripts/Character/Stats/MonsterRace.cs`) — Formless/Undead/Brute/Plant/Insect/Fish/Demon/DemiHuman/Angel/Dragon, using real Ragnarok Online's own race IDs so the raw numbers match the wiki — plus a new `CharacterStatsDefinition.Race` field (`Formless` first, so every asset authored before this field existed defaults to the one race no bonus below keys off, same "safe default" reasoning `MonsterSize.Medium` used) and a companion `CharacterStatsDefinition.IsBoss` flag for the Hiding exception below. `IDamageable` gained a matching `Race` property (`HealthComponent.Race => StatsHolder.Stats.Race`), so any attacker or defender can read a target's/attacker's race the same generic way `Size` already works.

Four real skills and one existing mechanic were unblocked in the same pass, all authored/wired together since they're the same kind of addition:

- **Demon Bane / Divine Protection (Acolyte passives)** — `SkillDefinition` gained `passiveRaceDamageBonusPerLevel`/`passiveRaceDefenseBonusPerLevel`, and `PlayerPassiveSkillController` gained matching `GetRaceDamageBonus(MonsterRace)`/`GetRaceDefenseBonus(MonsterRace)` methods (summed across learned passives, zero unless the race argument is Demon or Undead — never weapon-gated, matching real RO). Unlike every other passive bonus in this project, these are **per-hit, not folded into `CurrentSubStats`**, since they depend on which specific target/attacker is involved: `PlayerStatsController.GetRaceDamageBonus`/`GetRaceDefenseBonus` expose them publicly, and `PlayerCombatController.DealHit` / `PlayerSkillCaster.TryCastDamage`/`ApplyAreaDamage` add the damage bonus in for a Physical hit against a Demon/Undead target. The defense side needed one more piece of plumbing: `IDefensiveStatsProvider` gained `GetRaceDefenseBonus(MonsterRace attackerRace)`, and `HealthComponent.TakeDamage` now resolves the attacker's race (`attacker.GetComponentInParent<IDamageable>()?.Race`) and adds the bonus into physical defense only (matching real RO's "soft DEF," which never mitigates magical damage) — this required `EnemyAttackState.PerformAttack` to start passing `attacker: enemy.transform` into `TakeDamage`, which it previously omitted entirely (harmless: nothing else reacts to the player's own `AttackedBy` event).
- **Decrease AGI (Acolyte debuff)** — pure data, no new mechanic: a normal Enemy-targeted `SkillEffectType.Buff` skill using `buffStatBonusPerLevel`'s existing AGI channel with a negative value, plus item 7's resist-chance field. Real RO's own Decrease AGI isn't race-restricted at all (unlike this document's own earlier — incorrect — note that it was), so it needed nothing from Race; grouped into this pass anyway since it's the same "Acolyte curse-type debuff" family as Signum Crucis below.
- **Signum Crucis (Acolyte AoE debuff)** — the first skill to combine `SkillTargetType.AreaAroundCaster` with `SkillEffectType.Buff` rather than Damage, so `PlayerSkillCaster.TryCastBuff` gained a dispatch to a new `TryCastAreaDebuff`: an `OverlapSphere` around the caster (reusing `enemyLayer`/`AreaRadius`, same as `TryCastReveal`) that rolls item 7's resist chance separately against every distinct, living Demon/Undead target found and applies a DEF-down `BuffPayload` (reusing the existing `DefPercent` channel Provoke already established) to each one that fails to resist. `ponytail:` comment flags the Demon/Undead race filter as hardcoded rather than a new per-skill race-filter field, since Signum Crucis is the only area debuff today. `TryCastBuff`'s own payload-building code was extracted into a shared `BuildBuffPayload` helper so it and `TryCastAreaDebuff` don't duplicate the same field-by-field construction.
- **Heal's Undead-damage interaction** — real RO's Heal inflicts damage (half the would-be heal amount) instead of healing when cast on an Undead-race target. `PlayerSkillCaster.TryCastHeal` gained a `GetUndeadTargetInRange` check: if the currently selected target is a living Undead in range, it's damaged instead of the caster being healed — reusing the already-selected `targetSelector` rather than routing Heal through the enemy-target-picking flow (`NeedsEnemyTarget` still deliberately excludes Heal, unchanged). Ships as plain `Neutral`/Magical damage rather than real RO's Holy element — the same open question item 9 already tracks for Ruwach's damage, not resolved here either (`ponytail:` comment on the cast).
- **Boss/Insect/Demon exception on Hiding detection (item 4)** — `EnemyController` gained a `SeesThroughHiding` property (`Stats.IsBoss || Stats.Race is Insect or Demon`), checked alongside `IsPlayerHidden` in both `DetectPlayer()` and `EnemyWanderState.TryReaggroRememberedPlayer`, matching real Ragnarok Online's own Hiding-detection exceptions.

**Poring/Poporing tagged Plant race** (`race: 3` in `PorigonStats.asset`/`PoporigonStats.asset`), matching real RO — the only two enemy assets that exist in this project today, so nothing currently exercises the Demon/Undead-conditional bonuses above in actual gameplay; they're ready the moment an Undead or Demon enemy is authored. **Not done by this pass, deliberately**: prerequisite skill-ranks (real Demon Bane needs Divine Protection Lv. 3) aren't modeled, matching every other unmodeled skill prerequisite in this project (Berserk/Fatal Blow's Job Level gates); Signum Crucis's duration (15s flat) and area radius (8m, standing in for "screen-wide") are both invented placeholders, not wiki-sourced, same convention as Fatal Blow's invented stun duration; Decrease AGI's real move-speed-reduction half isn't modeled, the same `BuffPayload`-has-no-movement-channel gap Increase AGI already documented.

### 9. Elements this project's `Element` enum doesn't cover — open question
Current `Element` enum: `Water, Fire, Grass, Ground, Electric, Neutral` — a deliberately simplified set, not a direct port of Ragnarok Online's own ten-element wheel (Neutral/Water/Earth/Fire/Wind/Poison/Holy/Shadow/Ghost/Undead). Fire and Water map cleanly; Ground stands in for Earth; Electric is the closest analog to Wind. **Resolved for the Mage damage kit**: Lightning Bolt and Thunderstorm both use `Electric`, and their description text says "Electric-element" rather than "Wind-element" — the flavor name follows the actual system element a player can see resisted/exploited, not RO's own lore name for the skill. Three real skills still don't have any reasonable existing value to reuse: **Soul Strike**/**Napalm Beat** (Ghost), **Holy Light**/Heal's undead-damage/**Ruwach** (Holy — Ruwach itself is now authored, see "Acolyte kit" below, but ships its damage as plain `Neutral` until this is resolved; Heal's new Undead-damage interaction, item 8, ships Neutral for the same reason), **Envenom** (Poison, as an element — distinct from the Poison *status effect* in item 1, which Envenom's poison-inflict chance now uses; Envenom's damage still ships as plain `Physical`/`Neutral` in "Thief kit" below until this is decided). This still needs a decision when Acolyte/Thief's remaining skills get built: add `Ghost`/`Holy`/`Poison` as new `Element` values (cheap — the enum's raw-int Unity serialization only requires appending, never inserting, exactly like every other append-only enum in this project) versus deliberately folding them into `Neutral` to keep the element wheel small on purpose. Flagging now so it isn't decided by accident mid-implementation.

### 10. Generalize flat passive stat bonuses beyond ATK — IMPLEMENTED, 2026-09-17
`PlayerPassiveSkillController` no longer only sums a flat Status ATK bonus. `GetAttackBonus` was refactored onto a shared `SumPassiveBonus(equippedWeaponSubtype, getBonus)` private helper (same learned-skills iteration + `EffectType == Passive` + `MatchesWeaponRequirement` gate, just parameterized by which per-level getter to sum), and four new methods reuse it: `GetDexBonus`, `GetHitBonus`, `GetRangeBonus` (returns `float`, matching how `SkillDefinition.Range` itself is a float) and `GetFleeBonus`. `SkillDefinition` gained matching fields — `passiveDexBonusPerLevel`, `passiveHitBonusPerLevel`, `passiveRangeBonusPerLevel`, `passiveFleeBonusPerLevel` — and getters, mirroring `passiveAttackBonusPerLevel`/`GetPassiveAttackBonus` exactly (same `Tooltip` style, same `Mathf.RoundToInt(perLevel * skillLevel)` shape for the int-valued ones). **Item 8's race-conditional bonuses (Demon Bane/Divine Protection) deliberately do NOT go through this same generalization** — they're summed by their own dedicated `GetRaceDamageBonus`/`GetRaceDefenseBonus` methods instead, since (unlike every bonus here) they depend on a specific target/attacker at hit-resolution time rather than being a flat, always-on value safe to fold into `CurrentSubStats`.

`PlayerStatsController` wires all four in: the DEX bonus is folded into `effectiveStats` as a third `JobBonusStatsView` layer (reusing the same decorator the Job Level bonus and buff stats already use — no new provider type needed), so it flows through `SubStatsCalculator` automatically and boosts ranged Status ATK/Hit/Flee wherever DEX already feeds those formulas; the Hit and Flee bonuses are both added directly in `CurrentSubStats`, the same way the ATK passive bonus already was; the range bonus is exposed as a new public `GetPassiveRangeBonus()` method, read by `PlayerCombatController.GetAttackRange()` and added on top of the equipped weapon's own range. This unblocked **Owl's Eye**/**Vulture's Eye** (Archer) and **Improve Dodge** (Thief, flat Flee, not weapon-gated — see "Thief kit" below).

### 11. Displacement / knockback
Blocks **Arrow Repel** (Archer — pushes target back) and **Back Slide** (Thief — pushes self back). Both are the same primitive: move a transform N cells along a direction, respecting whatever obstacle/NavMesh checks movement already goes through. Small standalone utility, not worth a "system" — a static helper is enough. **Still open**: Arrow Repel was authored as a plain single-target Damage skill without its knockback half (see "Archer kit" below) rather than guessing at a cross-cutting `IDamageable.ApplyKnockback(...)` addition blind — that would touch every `IDamageable` implementer (player `HealthComponent`, every enemy prefab's wiring) and deserves its own pass with those files actually open, not a bolt-on here. Sketch for when that pass happens: a static `KnockbackUtility.Apply(NavMeshAgent agent, Vector3 sourcePosition, float distance)` that computes the push direction, samples a valid `NavMesh` point at that distance, and `agent.Warp(...)`s there — cheap, no coroutine, no physics involved, since RO's own knockback is an instant reposition, not a tween. Back Slide (Thief) is the same primitive applied to the caster instead of a target, still deferred for the same reason.

### 12. Skills with a Zeny cost
Blocks only **Mammonite** among these five classes' kits. Trivial: add `zenyCost` to `SkillDefinition`, checked via `PlayerCurrency.TrySpend` alongside the existing mana-cost check in `PlayerSkillCaster`. Not worth grouping as a "system" — just a field, mentioned here so it isn't missed. **Still not built** — `Mammonite.asset` is a plain Damage skill today with no Zeny cost, matching real RO's damage side only. Deliberately left out of this pass along with the rest of Merchant's kit (see the class-exclusive section below): every other Merchant skill needs either this, the NPC shop system, or the full cart/vending economy, so nothing else in Merchant's kit could be authored today without picking one of those up first.

## Mage damage kit — IMPLEMENTED, 2026-09-14

Fire Bolt, Cold Bolt, Lightning Bolt, Fire Ball, Thunderstorm and Fire Wall are now authored as real skill assets under `Assets/Data/Skills/Mage/`, registered in both `MageBase.asset` (the Mage's own `SkillDatabase`, read by the Skill Book window) and the master `SkillDatabase.asset` (used by the save system to resolve a learned/hotbarred skill by id). Chosen as the first real Mage skills specifically because every mechanic they need already existed before this pass — no new shared system, just tuning:

- **Fire Bolt / Cold Bolt / Lightning Bolt**: plain single-target `SkillEffectType.Damage`, `damageMultiplierPerLevel: 1` — real RO's "launch 1~10 bolts, each MATK×1" maps exactly onto this project's `CalculateDamage = MATK × multiplier × level` with no compression needed, unlike most other ported skills. Water / Fire / Electric respectively (Electric standing in for RO's own Wind, per item 9 above).
- **Fire Ball / Thunderstorm**: `TargetType.AreaAroundTarget` + `IsAreaOfEffect`, `AreaRadius: 4` (same "5×5 area" radius convention as Magnum Break). Fire Ball's `damageMultiplierPerLevel` (0.68) is linearly compressed to reach real RO's level-10 upper bound (MATK×3.4) at this project's level-5 cap — the same convention `MagnumBreak.asset` already established, so it undershoots real's level-1 minimum the same way Magnum Break does. Thunderstorm needed no compression: real's "1~10 hits, each MATK×1" collapses onto this project's single-hit-per-cast model the same way the bolts' formula does.
- **Fire Wall**: this project's first `SkillEffectType.Zone` skill asset (the mechanic itself, `SkillZoneController`, already existed per item 5). `TargetType.Ground`, `AreaRadius: 1.5` (a small zone standing in for RO's directional wall shape, which this project's sphere-overlap zone can't represent), `damageMultiplierPerLevel: 0.1` reaching real's flat 50% MATK per tick at level 5, `ZoneDurationSeconds`/`ZoneDurationPerLevel` reaching 15s at level 5 (matching `SkillZoneController`'s own default duration). No visual prefab yet (`zonePrefab` left empty — logic-only, same as every other zone until a visual is authored).

**Not done by this pass, deliberately**: Soul Strike, Napalm Beat, Frost Diver, Stone Curse, Sight, Safety Wall, Increase SP Recovery and Energy Coat — each still blocked on an unbuilt system (Ghost element, status effects beyond stun, stealth, the melee/ranged-block query, or is class-exclusive) at the time. **Frost Diver, Stone Curse and Sight have since shipped — see "Mage kit — extended" below.** No icons yet for any of the six new skills — same shared-sprite-sheet gap tracked since the Swordman pass (still only 4 slices, already used by BasicHeal/Heal/Bash/Snipe). **Correction, 2026-09-18: the scene-wiring caution below no longer applies to how skills actually reach the Skill Book window.** This paragraph originally read: "Scene wiring (`SkillBookWindowUI.allSkills` in `Prototype_Map01.unity`) is a manual Editor step, not attempted here — same reasoning as every other scene-YAML caution already documented in this project: too much serialized surface for a blind hand-edit." That described an `allSkills` field on `SkillBookWindowUI` that no longer exists in the live code — confirmed directly in `SkillBookWindowUI.cs`, which resolves its skill list through a `[SerializeField] ClassSkillDatabaseLookup skillDatabaseLookup` and `classController.CurrentClass`, then reads `database.AllSkills` from the resolved per-class `SkillDatabase` (e.g. `MageBase.asset`). A new skill only needs to be added to its class's `*Base.asset` `allSkills` array (a data asset, not the scene) — no `Prototype_Map01.unity` edit, manual or otherwise, is needed for a newly authored skill to appear in the Skill Book. Every skill listed in this document's "kit" sections was already reachable this way at the time it was authored.

### Skill description text sync, 2026-09-14
Several skill assets across every class still had placeholder description text (e.g. `"Fire Bolt Skill"`, `"Bash Skill"`) left over from when they were first stubbed out. Fixed to real, sourced text (iRO Wiki, consulted September 2026) for **FireBolt, Bash, StrongHeal, BasicHeal, Mammonite and Envenom**, each trimmed to describe only what's actually implemented today (no Zeny cost on Mammonite's text; Envenom's poison chance later added back to its text once that mechanic actually shipped, see "Thief kit" below), rather than a verbatim wiki copy-paste. (`StrongHeal`/`BasicHeal` were the working names at the time — see the roster audit below for their later rename/removal.)

**Found in passing, not fixed at the time**: `Snipe.asset` (Archer)'s name didn't correspond to any real base-Archer skill — the iRO Wiki lists no skill called "Snipe" for base Archer; the closest real analog is Double Strafe. **Resolved in the Archer kit below**: renamed to Double Strafe.

## Mage kit — extended, 2026-09-18

Frost Diver, Stone Curse and Sight are now authored as real skill assets under `Assets/Data/Skills/Mage/`, registered in both `MageBase.asset` and the master `SkillDatabase.asset`, alongside the original "Mage damage kit" above. All three use infrastructure that already existed and was already confirmed wired at runtime (items 1, 4 and 7) — no new mechanic except Reveal (item 4), which this pass is Sight's reason for existing:

- **Frost Diver**: plain single-target `SkillEffectType.Damage`, Water element, `damageMultiplierPerLevel: 0.4` (reaching real RO's level-10 upper bound, MATK×2.0, at this project's level-5 cap — same linear-compression convention as Fire Ball). `inflictedStatus: Freeze`, `successChancePerLevel: 0.13` (reaching real's level-10 upper bound, 65%, at level 5) and `inflictedStatusDurationPerLevel: 6` (reaching real's level-10 upper bound, 30s, at level 5). No Boss/Undead freeze-immunity exception (real RO's own immunity list isn't modeled, even now that Race/Boss exist — only the Hiding-detection exception in item 8 actually reads them) and no element-conversion-to-Earth (real RO petrifies Stone Curse's target to Earth property, not Frost Diver's — that one's still open regardless per item 9).
- **Stone Curse**: plain single-target `SkillEffectType.Damage` with `damageMultiplierPerLevel: 0` — a pure status-chance skill with zero direct damage, which item 7's `TryProcInflictedStatus` already supported without any code change since it fires regardless of how much damage a hit actually dealt. `inflictedStatus: Petrify`, `successChancePerLevel: 0.12` (reaching real's level-10 upper bound, 60%, at level 5). Real RO's HP-drain-while-petrified and element-conversion-to-Earth aren't modeled — this project's Petrify is a plain `IsImmobilized` timer like Stun/Freeze, same as every other status in item 1.
- **Sight**: this project's first `SkillEffectType.Reveal` skill asset (the mechanic itself, `PlayerSkillCaster.TryCastReveal`, shipped as part of this pass — see item 4). `TargetType.Self`, `maxLevel: 1`, `damageMultiplierPerLevel: 0` (reveal only, no damage, matching real RO), `AreaRadius: 5.5` (standing in for real's 7×7 grid, proportionally larger than Fire Ball's 5×5-area radius of 4).

**Not done by this pass, deliberately**: Soul Strike, Napalm Beat (both item 9, Ghost element), Safety Wall (item 5's melee-block half), Increase SP Recovery, Energy Coat (class-exclusive). No icons yet for any of the three new skills — same shared-sprite-sheet gap as every other kit.

## Archer kit — IMPLEMENTED, 2026-09-17

Double Strafe, Arrow Shower, Arrow Repel, Improve Concentration, Owl's Eye and Vulture's Eye are now authored as real skill assets under `Assets/Data/Skills/Archer/`, registered in both `ArcherBase.asset` and the master `SkillDatabase.asset`. All six mechanics needed already existed or shipped as part of this same pass (item 10, passive generalization) — no per-skill new system beyond that one:

- **Double Strafe**: `Snipe.asset` renamed in place (same GUID, so no re-registration needed) rather than kept as an invented name. `damageMultiplierPerLevel: 0.76`, reaching real RO's level-10 upper bound (380%) at this project's level-5 cap — same linear-compression convention as Fire Ball. This project doesn't model a skill hitting more than once per cast, so the real "two arrows" flavor is approximated as a single higher-multiplier hit, same simplification already applied to Thunderstorm's multi-hit.
- **Arrow Shower**: `TargetType.AreaAroundTarget` + `IsAreaOfEffect`, `AreaRadius: 4` (same convention as Fire Ball/Thunderstorm/Magnum Break), `damageMultiplierPerLevel: 0.5` reaching real's level-10 upper bound (250%) at this project's level-5 cap.
- **Arrow Repel**: plain single-target `SkillEffectType.Damage`, `maxLevel: 1` (real RO is a fixed-effect quest skill, not a leveled one in this project's sense), `damageMultiplierPerLevel: 1.5` matching real's flat 150%. **Its knockback half is not implemented** — see item 11 above for why and the sketch for later.
- **Improve Concentration**: `SkillEffectType.Buff`, `TargetType.Self`, `buffStatBonusPerLevel` = +3 AGI/+3 DEX per level (reaching +15/+15 at this project's level-5 cap), `buffDurationPerLevel: 48` (reaching real's 240s at level 5, `buffDurationSeconds` left at 0). **Documented simplification**: real RO's effect is +12% DEX/AGI (a percentage), but `BuffPayload` (item 2) has no percentage-stat channel today, only flat STR/AGI/VIT/INT/DEX/LUK — approximated as a flat bonus, same as every other buff skill authored against this payload. The reveal-hidden-enemies half is still not implemented — item 4's `Reveal` effect type (now shipped, see "Mage kit — extended" above) is the natural fit, but Improve Concentration is a Buff skill today, not a Reveal one, so combining the two would need its own small design pass rather than a one-line change.
- **Owl's Eye**: `SkillEffectType.Passive`, `passiveDexBonusPerLevel: 2` reaching real's +10 DEX at this project's level-5 cap. Not weapon-gated (`PassiveRequiredWeaponSubtypes` left empty), matching real RO.
- **Vulture's Eye**: `SkillEffectType.Passive`, `passiveHitBonusPerLevel: 2` and `passiveRangeBonusPerLevel: 0.4` (reaching real's +10 Hit and roughly +2 range at this project's level-5 cap), gated to `WeaponSubtype.Bow` via `PassiveRequiredWeaponSubtypes`, matching real RO.

**Not done by this pass, deliberately**: Arrow Crafting (class-exclusive, low reuse, same reasoning as every other class-exclusive deferral in this plan — Merchant's cart, Acolyte's Teleport). No icon yet for any of the five new skills — same shared-sprite-sheet gap as the Mage kit.

## Acolyte kit — IMPLEMENTED, 2026-09-17, extended 2026-09-18, extended again 2026-09-18

Blessing, Increase AGI, Angelus and Ruwach are now authored as real skill assets under `Assets/Data/Skills/Acolyte/`, registered in both `AcolyteBase.asset` and the master `SkillDatabase.asset`, alongside the already-existing `StrongHeal.asset` (internally renamed to Heal in the roster audit below) and the shared `BasicHeal.asset`:

- **Blessing**: `SkillEffectType.Buff`, `TargetType.Ally` (self-only until item 3/party exists), `buffStatBonusPerLevel` = +1 STR/+1 INT/+1 DEX per level (reaching +5/+5/+5 at this project's level-5 cap, matching real RO's level-10 upper bound), flat `buffDurationSeconds: 300` (real RO's duration doesn't meaningfully scale with level in the classic era, so no per-level component).
- **Increase AGI**: `SkillEffectType.Buff`, `TargetType.Ally`, `buffStatBonusPerLevel` = +2 AGI per level (+10 at level 5) and `buffAspdPercentPerLevel: 0.03` (+15% Aspd at level 5). **Documented simplification**: real Increase AGI also grants a movement-speed bonus, but `BuffPayload` has no movement-speed channel — left out entirely rather than approximated, since there's no existing flat/percent field this project already tracks that a movement bonus could piggyback on the way DEX/AGI's percentage-to-flat conversion could for Improve Concentration.
- **Angelus**: `SkillEffectType.Buff`, `TargetType.Ally`, `buffDefPercentPerLevel: 0.03` (+15% Def at level 5) and `buffMaxHealthPerLevel: 20` (+100 flat Max HP at level 5 — `SkillDefinition`'s own tooltip for `buffMaxHealthPerLevel` already named Angelus as its intended use case when item 2 was built, so this was the field's first real consumer).
- **Ruwach, 2026-09-18**: this project's second `SkillEffectType.Reveal` skill asset (after Sight, see "Mage kit — extended" above), and the first to also damage what it reveals. `TargetType.Self`, `maxLevel: 1`, `damageMultiplierPerLevel: 1.45` (matching real's flat 145% MATK), `AreaRadius: 4` (matching real's 5×5 grid, same radius convention as Fire Ball). Damage ships as plain `Neutral` — real Ruwach's damage is Holy-element, still an open question per item 9.
- **Demon Bane / Divine Protection / Decrease AGI / Signum Crucis, 2026-09-18**: item 8's four Race-unblocked skills — see item 8 above for the full mechanical writeup (new race-conditional passive bonus methods, the AoE-debuff cast path, Decrease AGI's plain data authoring). All four registered the same way as every skill above.

**Not done by this pass, deliberately**: real party propagation for Blessing/Increase AGI/Angelus (item 3 — the self-only cast is the documented stopgap), Cure (item 1, still needs a status-clearing mechanism), Holy Light (item 9, Holy element), Pneuma (item 5's melee/ranged-block half), Teleport/Warp Portal and Aqua Benedicta (class-exclusive). No icons yet for any of the eight new skills — same shared-sprite-sheet gap as every other kit above.

## Thief kit — IMPLEMENTED, 2026-09-17, extended 2026-09-18

Improve Dodge, Hiding, Sand Attack and Stone Fling are now authored as real skill assets under `Assets/Data/Skills/Thief/`, registered in both `ThiefBase.asset` and the master `SkillDatabase.asset`, alongside the already-existing `Envenom.asset`:

- **Improve Dodge**: `SkillEffectType.Passive`, `passiveFleeBonusPerLevel: 2` reaching real's +10 Flee at this project's level-5 cap. Not weapon-gated, matching real RO — this is exactly the case item 10 was generalized to cover (`PlayerPassiveSkillController.GetFleeBonus`, summed the same way as the others but never checked against the equipped weapon).
- **Hiding, 2026-09-18**: the first skill to actually consume item 4's stealth infrastructure, and the reason `SkillEffectType` gained a new `Toggle` value. `Hiding.asset` is `SkillEffectType.Toggle`, `TargetType.Self`, `maxLevel: 1` (no field scales with level — real RO's level affects detection resistance, which this project doesn't model yet). `PlayerSkillCaster.TryCastSkill`'s switch gained a `Toggle` branch calling the new `TryCastToggle`, which spends mana and flips the caster's own `StatusEffectController.IsHidden` via `SetHidden(!IsHidden)` — recasting the same skill turns it back off, matching how real Hiding is toggled. `ponytail:` comment on `TryCastToggle` flags it as hardcoded to Hiding specifically (the only Toggle skill today) rather than a generic "which flag" mechanism — generalize only if a second toggle status shows up. Same simplifications as every other skill here: flat mana cost paid on and off (real Hiding drains SP over time while active instead, which this project has no per-skill drain mechanism for) and no Boss/Insect/Demon detection exception on the *player's own* Hiding attempt — item 8's exception is enemy-side (whether an enemy sees through Hiding), which is now built and wired; real RO also gives higher-level monsters a chance to detect a low-skill-level Hiding regardless of race, which still isn't modeled.
- **Envenom's poison chance, 2026-09-18**: `Envenom.asset` gained `inflictedStatus: Poison`, `inflictedStatusDurationSeconds: 10`/`inflictedStatusDurationPerLevel: 2` (reaching 20s at this project's level-5 cap) and `poisonDamagePerTickPerLevel: 3` (reaching 15/tick at level 5, ticking every `poisonTickIntervalSeconds` — 1s by default). `successChancePerLevel: 0.1`, reaching real RO's level-10 upper bound (50%) at this project's level-5 cap — same linear-compression convention as Double Strafe/Fire Ball (real scales 14% at level 1 to 50% at level 10; this project's pure `chance = perLevel × level` formula has no flat base term, so the level-1 value undershoots real's 14% the same way Fire Ball undershoots real's level-1 minimum). Pure data change — `PlayerSkillCaster.TryProcInflictedStatus` already read exactly these fields (`InflictedStatus`, `GetInflictedStatusDuration`, `GetPoisonDamagePerTick`, `GetSuccessChance`) since item 7 shipped, so no code changed. Description text updated to mention the poison chance. Damage half still ships as plain `Physical`/`Neutral` — the Poison *element* (item 9) is still an open question, distinct from the Poison *status* this now uses.
- **Sand Attack, 2026-09-18**: plain single-target `SkillEffectType.Damage`, `maxLevel: 1` (real RO is a fixed-effect quest skill), `damageMultiplierPerLevel: 1.25` matching real's flat 125%, `element: Ground` (standing in for Earth, per item 9). `inflictedStatus: Blind`, `successChancePerLevel: 0.15` — an exact match for real's flat 15% chance since `maxLevel` is 1. Blind is applied but still has no accuracy-penalty consumer (item 1's documented gap) — this just widens who can apply it.
- **Stone Fling, 2026-09-18**: plain single-target `SkillEffectType.Damage`, `maxLevel: 1`, Neutral element, `damageMultiplierPerLevel: 0.5`. `inflictedStatus: Stun`, `successChancePerLevel: 0.05` (exact match for real's flat 5% chance). **Two documented simplifications**: real RO's damage is a fixed 50, independent of stats — this project has no "true damage" field (same gap item 1 already notes for Poison), so it's approximated as a small ATK-scaled hit instead of adding a single-use field; and real RO's 5% roll picks between Stun *or* Blind — this project's `SkillDefinition` only carries one `InflictedStatus` per skill, so Stun was picked (Blind is already covered by Sand Attack above) rather than adding a second inflicted-status field for one skill.

**Not done by this pass, deliberately**: Double Attack (item 7's chance mechanic doesn't apply — this is a proc on the player's own basic attack, a new mechanism, not skill-cast reuse), Envenom's element (item 9), Steal (class-exclusive), Back Slide (item 11, same knockback primitive Arrow Repel needs). No icon yet for any of the five skills above — same gap as every other kit above.

## Skill roster audit against iRO Wiki reference, 2026-09-17

Every skill asset in the project (all ~35, across all six classes) was cross-referenced against the real Ragnarok Online skill trees (iRO Wiki, consulted September 2026) — the same reference this whole document already cites, not the project's own `MMO_Project_Design_Data_Tracker.xlsx` (checked and ruled out: its "Skills" sheet only lists 4 early items explicitly labeled "Test Content," not a design reference). Result: every Swordman, Mage, Archer, Merchant and Thief skill already authored matches a real skill name and class for that skill tree — no changes needed there. Two Acolyte-adjacent findings:

- **`BasicHeal.asset`** (guid `1cc33e3d848183b42b028ef4b8def3d6`) — a shared "any class can cast a basic heal" skill that doesn't correspond to any real Ragnarok Online skill (in real RO, only Acolyte's tree has a Heal skill). Flagged as non-canonical, but **kept by Victor's explicit decision, 2026-09-17** — still registered in every `*Base.asset` (`SwordmanBase`, `MageBase`, `MerchantBase`, `ArcherBase`, `ThiefBase`, `AcolyteBase`) and the master `SkillDatabase.asset`, unchanged. Treated as this project's own deliberate universal utility skill rather than a strict RO port.
- **`StrongHeal.asset` renamed internally to "Heal"** (guid `023c463efa310c44397ea4dd4bb226d6`, filename unchanged) — real RO's actual Acolyte skill is just "Heal," not "Strong Heal." `skillName`/`m_Name` updated to `Heal`; `description` rewritten to no longer reference Basic Heal by name.

## Class-exclusive features (touch one class only, still worth naming so nothing's forgotten)

- **Merchant**: Pushcart/Vending/Cart Revolution/Cart Decoration/Change Cart/Open Buying Store — a whole player-shop-and-cart economy layer, RO's own peer-to-peer trading system. Large, essentially Merchant-only, likely its own multi-pass feature whenever Merchant's turn comes rather than something to front-load. **Discount**/**Overcharge** (NPC shop price modifiers) depend on whatever NPC shop system exists (not investigated here — separate check needed when Merchant is actually built). **Item Appraisal** needs an "unidentified item" flag on items/inventory that doesn't exist yet. **Enlarge Weight Limit** is the one easy piece — `Inventory.MaxCarryWeight` is a plain constructor value today; giving it the exact same optional-bonus-provider hook `IMaxHealthBonusProvider` already established would cover this with almost no new code. Merchant's kit is otherwise unchanged this pass (see item 12) — every remaining Merchant skill needs one of these, so nothing else could be authored today without picking one of those up first.
- **Acolyte**: **Teleport**/**Warp Portal** — a player-cast, memorized-location fast-travel skill. Distinct from the already-built `Project.World`/`WarpPortal` inter-map system (that's fixed, scene-authored portals; this is player-triggered, to a remembered point). **Aqua Benedicta** (crafting holy water near water) is minor and easy to defer.
- **Thief**: **Steal** — RO's own steal-chance-vs-monster formula and one-steal-per-monster tracking; ties into loot but is otherwise Thief-only.
- **Archer**: **Arrow Crafting** — converting materials into ammo; a small standalone recipe/conversion feature, low reuse elsewhere. Still deferred after the Archer kit above.
- **Mage**: **Energy Coat** — a toggled, SP-draining damage-reduction buff. Shape-wise this is close to Berserk's controller (a component watching a condition and driving a persistent `BuffController` entry) but drains SP on a timer instead of watching HP — can likely reuse most of that pattern once Berserk's code exists as a reference (it already does, per this session's earlier work).

## Suggested build order

1. ~~**Status effects, stun only**~~ (item 1) — **IMPLEMENTED**, then **widened to Poisoned/Silenced/Blinded/Frozen/Petrified, 2026-09-17** (Blind's accuracy hook still deferred, see item 1 above).
2. ~~**Chance-based skill effects**~~ (item 7) — **IMPLEMENTED, 2026-09-17.**
3. ~~**Stealth/detection**~~ (item 4) — **IMPLEMENTED, 2026-09-18**, including its `Reveal` effect type and (as of the Race pass below) its Boss/Insect/Demon detection exception.
4. ~~**Author every skill blocked only on already-shipped, already-wired infrastructure**~~ — **DONE, 2026-09-18**: Envenom's poison chance, Frost Diver, Stone Curse, Sight, Sand Attack, Stone Fling and Ruwach all shipped in one pass (see the per-class kit sections above). This closes out every skill in the roster that needed nothing but authoring against items 1/4/7.
5. ~~**Race**~~ (item 8) — **IMPLEMENTED, 2026-09-18**: `MonsterRace` enum + `CharacterStatsDefinition.Race`/`IsBoss`, wired into Demon Bane, Divine Protection, Decrease AGI, Signum Crucis, Heal's Undead-damage interaction, and Hiding's Boss/Insect/Demon detection exception. Poring/Poporing tagged Plant. See item 8 above for the full writeup.
6. **Party support** (item 3) — now the next biggest open prerequisite: turns Blessing/Increase AGI/Angelus's already-shipped self-only casts into real party buffs.
7. A small "clear a status" mechanism (item 1's `Cure`/`Detoxify` gap) — the one remaining shape item 1's infrastructure doesn't cover at all (everything else inflicts a status; nothing removes one).
8. **Melee/ranged-block query** (item 5's remaining half, for Safety Wall/Pneuma).
9. Everything else (items 9, 11, 12) are one-or-two-skill unlocks each — build them opportunistically per class rather than ahead of need, same YAGNI reasoning `CLASSES_SWORDMAN.md` already applied to its own gaps. Item 11 (knockback) is the next concrete blocker for both Arrow Repel and Back Slide; item 12 (Zeny cost) is the next concrete blocker for Merchant specifically.

## Per-class skill → system mapping

### Mage
| Skill | Real effect | Depends on |
|---|---|---|
| Fire Bolt / Cold Bolt / Lightning Bolt | MATK×level, single target, elemental | **Implemented** — `FireBolt.asset`/`ColdBolt.asset`/`LightningBolt.asset` |
| Fire Ball | Fire AoE around target | **Implemented** — `FireBall.asset` (item 6) |
| Fire Wall | Persistent damaging wall | **Implemented** — `FireWall.asset` (item 5) |
| Thunderstorm | Wind AoE around target, multi-hit | **Implemented** — `Thunderstorm.asset` (item 6) |
| Frost Diver | Water damage + freeze | **Implemented** — `FrostDiver.asset` (item 1) |
| Stone Curse | Petrify, zero direct damage | **Implemented** — `StoneCurse.asset` (item 7); element-conversion-to-Earth not modeled |
| Sight | Reveal hidden in area | **Implemented** — `Sight.asset` (item 4, `SkillEffectType.Reveal`) |
| Soul Strike / Napalm Beat | Ghost-element, splash on Napalm Beat | Item 9 (Ghost) + item 6 (done, Napalm Beat's splash) |
| Safety Wall | Melee-blocking zone | Item 5 (zone core done, melee-block query still needed) |
| Increase SP Recovery | Passive SP regen boost | Same regen-over-time mechanism as `CLASSES_SWORDMAN.md` item 3 (implemented for HP), extended to SP |
| Energy Coat | Toggled SP-drain damage reduction | Class-exclusive, see above |

### Merchant
| Skill | Real effect | Depends on |
|---|---|---|
| Mammonite | Melee damage + Zeny cost | Damage half **implemented** (`Mammonite.asset`, description synced); Zeny cost still needs item 12 |
| Discount / Overcharge | NPC price modifiers | Class-exclusive (NPC shop system, unverified) |
| Enlarge Weight Limit | +carry weight | Class-exclusive, but easy (provider-hook pattern) |
| Item Appraisal | Identify items | Class-exclusive (needs "unidentified" item flag) |
| Pushcart / Vending / Cart Revolution / Cart Decoration / Change Cart / Open Buying Store | Cart + player-shop economy | Class-exclusive, large, own feature pass |
| Crazy Uproar | Party ATK/STR buff | Item 3 |

### Acolyte
| Skill | Real effect | Depends on |
|---|---|---|
| Heal | HP restore, bonus vs. Undead | **Implemented, both halves** (`StrongHeal.asset`, internally renamed to Heal — see roster audit; `BasicHeal.asset` kept as a deliberate any-class utility, not a strict RO port) — item 8's Race shipped the Undead-damage half, 2026-09-18 (ships Neutral element, not Holy, per item 9) |
| Blessing | Flat STR/DEX/INT, self + party | **Implemented**, self-only — `Blessing.asset` (item 2 + item 3 for real party) |
| Increase AGI | Flat AGI + ASPD% + move speed, self + party | **Implemented**, self-only, no move speed — `IncreaseAGI.asset` (item 2 + item 3 for real party) |
| Angelus | DEF% + flat Max HP, self + party | **Implemented**, self-only — `Angelus.asset` (item 2 + item 3 for real party) |
| Ruwach | Reveal hidden + Holy damage | **Implemented** — `Ruwach.asset` (item 4, `SkillEffectType.Reveal`); damage ships Neutral until item 9 |
| Cure | Removes silence/blind/chaos | Item 1's infrastructure only inflicts statuses today — needs a new clear mechanism (see suggested build order item 7) |
| Decrease AGI | Chance-based -AGI debuff, any race | **Implemented** — `DecreaseAGI.asset` (item 7); not race-restricted in real RO, despite this document's earlier note |
| Signum Crucis | AoE DEF-down debuff vs. Demon/Undead | **Implemented** — `SignumCrucis.asset` (item 7 + item 8, Race); duration/radius are invented placeholders |
| Demon Bane | Flat physical damage bonus vs. Demon/Undead | **Implemented** — `DemonBane.asset` (item 8, Race + item 10's passive infrastructure) |
| Divine Protection | Flat physical defense bonus vs. Demon/Undead | **Implemented** — `DivineProtection.asset` (item 8, Race + item 10's passive infrastructure) |
| Holy Light | Holy damage | Item 9 (Holy element) |
| Pneuma | Ranged-blocking zone | Item 5 (zone core done, ranged-block query still needed) |
| Teleport / Warp Portal | Fast travel | Class-exclusive, see above |
| Aqua Benedicta | Craft holy water | Class-exclusive, minor |

### Thief
| Skill | Real effect | Depends on |
|---|---|---|
| Improve Dodge | Flat Flee | **Implemented** — `ImproveDodge.asset` (item 10) |
| Double Attack | Proc chance, double damage w/ dagger | Item 7 + weapon-gating already established |
| Envenom | Poison damage + poison chance | **Implemented** — damage half + poison-inflict chance both ship (`Envenom.asset`, item 1 + item 7); poison element (item 9) still open |
| Steal | Steal item from monster | Class-exclusive |
| Hiding | Become hidden | **Implemented** — `Hiding.asset` (item 4, `SkillEffectType.Toggle`) |
| Sand Attack | Earth damage + blind chance | **Implemented** — `SandAttack.asset` (item 1 + item 7); element approximated as Ground |
| Stone Fling | Low fixed damage + stun/blind chance | **Implemented** — `StoneFling.asset` (item 1 + item 7); fixed damage approximated as ATK-scaled, stun-only (not stun-or-blind) |
| Detoxify | Cure poison | Same gap as Cure above — needs a clear mechanism |
| Back Slide | Self-displacement | Item 11 |

### Archer
| Skill | Real effect | Depends on |
|---|---|---|
| Double Strafe | Single-target damage | **Implemented** — `Snipe.asset` renamed to Double Strafe |
| Arrow Shower | AoE around target | **Implemented** — `ArrowShower.asset` (item 6) |
| Arrow Repel | Damage + knockback | Damage half **implemented** — `ArrowRepel.asset`; knockback still needs item 11 |
| Improve Concentration | DEX/AGI% buff + reveal hidden | Stat-buff half **implemented** (flat approximation, item 2); reveal half still needs its own design pass to combine with item 4's `Reveal` effect type |
| Owl's Eye | Flat DEX | **Implemented** — `OwlsEye.asset` (item 10) |
| Vulture's Eye | Hit/range with bow | **Implemented** — `VulturesEye.asset` (item 10) |
| Arrow Crafting | Craft ammo | Class-exclusive |
